# 박준호 | Data/Batch Backend Engineer

> 문제를 구조적으로 분석하고, 데이터 정합성과 배치 안정성을 개선하는 백엔드 개발자
설계부터 구현, 배포, 운영까지 전 과정을 책임지며 정량적 지표로 개선 효과를 증명합니다.

📧 junho6667@gmail.com　　📱 010-3525-6275　　🔗 github.com/junho0831　　🌐 so-dak.com

———

## 한눈에 보기

| 만 3년 9개월 (4년차) | 30.6% | 70% | 0건 |
| --- | --- | --- | --- |
| 백엔드·데이터 실무 경력 | 대용량 적재 시간 단축 (1,973만건) | 장애 분석 리드타임 단축 (40분→12분) | 마이데이터 정합성 오류 달성 (월 3건→0건) |

- Java/Spring Boot 기반 백엔드 아키텍처 및 Python/Airflow 대용량 배치 파이프라인 운영 (총 45개월)
- 약 1,973만 건 로그 처리 시간을 4,175초 → 2,896초로 30.6% 단축한 PostgreSQL 청크/COPY 파이프라인 개선
- Elasticsearch 검색 장애 DB Fallback, Redis TTL 세션 관리, Admin 재처리 API 기반 운영 복구 체계 구축
- Redis Lua Script 동시성 제어, DB Outbox 패턴, 원자적 상태 머신 설계 기반 실시간 서비스 구현

———

# 경력 (Work Experience)

———

## 엔셀 — Prism (Airflow 데이터 파이프라인)

📅 2025.01 ~ 현재 (재직 중, 21개월)

Python Airflow PostgreSQL FTP Server-side Cursor COPY Range Partition

### 🔴 문제

- Java 기반 FTP 파일 처리 배치에서 단일 지점 실패 시 어느 단계에서 오류가 났는지 추적이 어렵고 복구 비용이 컸음
- 수동 재실행이나 Airflow 재시도 시 원본 파일 삭제와 적재 순서가 어긋나 중복 적재 또는 데이터 유실 위험 존재
- 장비에서 발생하는 비정형 운영 로그(단위 표기 불일치, 줄바꿈 예외, 오타 등)로 인해 파싱 실패 시 배치 전체가 중단되는 문제
- 약 1,973만 건에 달하는 대용량 로그 적재 시 단일 트랜잭션 병목으로 처리 시간이 4,000초를 초과하여 후속 배치 지연 초래

### 🔵 해결

- Java 배치를 Python/Airflow DAG로 이관하며 다운로드 → 검증 → 변환 → DB 적재 → 업로드 → 원본 정리 단계로 Task 모듈화 (실패 단계부터 즉시 재실행 가능)
- ER Dose RAW warning 및 EUV root cause 파싱 경로를 분리하고, 로그 변형 예외 처리 및 개별 필드 실패 시 NULL 처리로 전체 row 유실 방지
- DB commit 및 업로드 성공 이후에만 FTP 원본을 삭제하도록 실행 순서를 고정하고, `source_file` Unique 제약 조건과 UPSERT 로직을 적용해 멱등성(Idempotency) 보장
- `code_occur_time` 기준 Range Partition에 청크별 COPY 적재를 적용하고, 서버사이드 커서 조회와 적재 파이프라인을 오버랩 실행하여 I/O 병목 해소

### 🟢 성과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 1,973만 건 대용량 적재 시간 | 4,175초 | 2,896초 (30.6% 단축) |
| 파일 파이프라인 재실행 정합성 | 중복/유실 리스크 존재 | 실패 단계 선별 재처리 및 멱등성 100% 확보 |
| 로그 파싱 안정성 | 단일 파싱 에러 시 전체 중단 | 개별 필드 fallback으로 정상 row 유실 방지 |

———

## 엔셀 — DataForge (사내 데이터 처리 & Spring API)

📅 2025.01 ~ 현재 (재직 중)

Java 17 Spring Boot PostgreSQL Redis Elasticsearch JWT OAuth2

### 🔴 문제

- 야근/근태 신청·조회 서비스에서 피크 타임 Elasticsearch 응답 지연 또는 장애 발생 시 핵심 조회 및 승인 업무가 중단되는 위험
- 검색 엔진(ES)과 관계형 DB(PostgreSQL) 간 데이터 구조 차이로 장애 대응 시 일관된 사용자 경험 제공의 한계
- 다중 인스턴스 환경에서 잦은 토큰 갱신 시 상태 불일치로 인한 세션 충돌 및 재로그인 불편 발생
- 데이터 불일치나 누락 발생 시 개발자가 DB에 직접 쿼리를 실행해 수동 복구해야 하는 운영 비효율

### 🔵 해결

- Elasticsearch 장애 감지 조건을 정의하고, 실패 시 서비스 중단 없이 PostgreSQL에서 조회하는 DB Fallback 자동 전환 구조 설계
- ES와 DB fallback 응답 포맷, 정렬 정책, 페이징 처리를 일원화해 fallback 전환 시에도 동일한 데이터 해석 보장
- Refresh Token 저장, 검증, 회전, 폐기를 Redis TTL 기준으로 단일화하여 분산 환경에서도 인증 상태 정합성 유지
- 신청 승인/반려, 재색인 트리거, 공통 예외 validation 응답을 연결한 Admin 운영 복구 API 구축

### 🟢 성과

- ES 장애 발생 시에도 서비스 중단 없이 DB Fallback을 통해 핵심 조회 가용성 100% 유지
- 세션 충돌 및 불필요한 재로그인 제거, Redis 기반 토큰 라이프사이클 관리 표준화
- 개발자 DB 수기 수정 대신 운영자 Admin API를 통한 즉각적인 데이터 복구 흐름 정착

———

## 엔셀 — SMIP (공통 예외 처리 & 회귀 테스트)

📅 2025.01 ~ 2025.12

Java Spring Boot Vue.js JUnit5 Mockito

### 🔴 문제

- API마다 예외 처리 방식과 오류 응답 포맷이 달라 장애 원인 파악 및 대응이 지연됨
- 과거에 해결했던 경계값 버그가 코드 수정 과정에서 반복 재발
- Vue.js 화면 상태 관리가 파편화되어 유지보수 및 인수인계 비용 증가

### 🔵 해결

- 공통 예외 처리 계층(GlobalExceptionHandler)과 표준화된 오류 응답 규격을 구축해 진입점 일원화
- 반복 발생하던 장애 케이스를 JUnit5/Mockito 회귀 테스트로 고정하여 배포 전 사전 검증 강화
- Vue.js 화면 상태를 MVVM 패턴과 공통 스토어로 집중시켜 UI와 비즈니스 로직 분리

### 🟢 성과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 장애 분석 리드타임 | 40분 | 12분 (70% 단축) |
| 동일 유형 장애 재발률 | 기준 | 30% 감소 |

———

## 헥토 — KMS (배포 자동화 & CI/CD 표준화)

📅 2023.10 ~ 2024.07 (10개월) · AI개발팀

Java Spring Boot GitLab CI/CD Docker Nginx

### 🔴 문제

- 수동 빌드/배포 환경으로 릴리스 리드타임이 길고, 개발자 로컬 환경과 서버 환경의 불일치로 인한 배포 실패 빈발

### 🔵 해결

- GitLab CI/CD와 Docker 컨테이너 기반으로 빌드, 테스트, 이미지 생성, 배포 파이프라인 구축
- Nginx 리버스 프록시 및 Staging 검증 환경을 표준화하여 무중단 전환 경로 마련

### 🟢 성과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 릴리스 소요 시간 | 1시간 | 25분 (58% 단축) |
| 배포 실패율 | 5% | 0% 달성 |

———

## 헥토 — SmartQ (사내 규정 RAG 검색 API)

📅 2023.10 ~ 2024.07 · AI개발팀

Python LangChain OpenAI API FastAPI Spring Boot

### 🔴 문제

- 기존 키워드 일치 검색 방식은 문맥을 반영하지 못해 사내 규정 탐색 시 상담/운영팀의 직접 확인 리소스 과다 소요

### 🔵 해결

- LangChain 및 OpenAI API를 활용한 RAG(Retrieval-Augmented Generation) 사내 규정 질의응답 API 모듈 구축
- 기존 레거시 시스템 변경을 최소화하도록 독립 마이크로서비스 형태의 질의응답 API 연동

### 🟢 성과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 질의 응답 정확도 | 60% | 80% (20%p 향상) |
| 답변 대기 시간 | 5분 | 1분 (80% 단축) |
| 상담/운영팀 업무 시간 | 기준 | 주당 12시간 절감 |

———

## 헥토이노베이션 — 세이프캐시 (마이데이터 정기 배치)

📅 2022.08 ~ 2023.09 (1년 2개월) · 마이데이터개발팀

Java Spring Boot MyBatis MySQL Crontab

### 🔴 문제

- 마이데이터 금융 거래 정기 동기화 배치가 수동 실행과 개발자 DB 확인에 의존하여 처리 지연 및 누락 리스크 상존

### 🔵 해결

- Crontab 기반 정기 배치 자동화 및 실행 이력·결과 로깅 체계 구축
- 이상 거래 및 동기화 실패 건을 개발자 개입 없이 운영자가 직접 조회·수정·재처리할 수 있는 Admin UI/API 연동

### 🟢 성과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 데이터 정합성 이슈 | 월 3건 | 0건 (완전 해소) |

———

# 프로젝트 (Projects)

———

## VoiceLink — 실시간 1:1 음성 통화 매칭 플랫폼

📅 2024 ~ 현재 · 개인 서비스 ([voice-link.co.kr](https://voice-link.co.kr))

Java 17 Spring Boot 3 Redis Lua Script DB Outbox PostgreSQL LiveKit WebRTC Docker Nginx

> 다중 인스턴스 환경에서 동시 매칭 경합 조건과 상태 불일치를 원자적 연산과 Outbox 패턴으로 해결한 실시간 음성 통화 서비스

### 🔵 핵심 문제 및 해결

- **원자적 매칭 상태 머신**: 동시 접속 사용자가 취소와 매칭을 동시에 요청할 때 발생하는 경합 조건을 방지하기 위해, Redis Lua Script를 활용해 대기열 탐색·제거·매칭 성사를 단일 원자적 트랜잭션으로 처리
- **DB Outbox 패턴을 통한 정합성 보장**: 매칭 성공 트랜잭션과 외부 WebRTC(LiveKit) 룸 생성 비동기 이벤트를 분리하여 외부 API 장애가 DB 트랜잭션 롤백으로 번지지 않도록 분리
- **무중단 운영**: Docker 컨테이너와 Nginx 리버스 프록시를 적용해 실서비스 운영 중

———

# 학력 (Education)

———

- **동명대학교 (부산)**: 로봇시스템공학과 학사 (`2015.03 ~ 2019.03` 졸업, 학점: 3.72 / 4.5)
- **해운대고등학교 (부산)**: 자연계열 (`2012.03 ~ 2015.02` 졸업)

———

GitHub: https://github.com/junho0831　|　Portfolio: https://so-dak.com/
