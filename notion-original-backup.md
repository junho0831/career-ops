# +ㄱ박준호 | 백엔드 개발자

> 문제를 구조적으로 분석하고, 운영 가능한 시스템으로 개선하는 백엔드 개발자
설계부터 구현, 배포, 운영까지 전 과정을 책임지며 정량적 지표로 개선 효과를 증명합니다.
> 

📧 junho6667@gmail.com　　📱 010-3525-6275　　🔗 github.com/junho0831

———

## 한눈에 보기

| 3년+ | 60% | 30% | 0% |
| --- | --- | --- | --- |
| 백엔드 개발 경력 | CI/CD 자동화로 배포 시간 단축 | 동일 유형 장애 재발률 감소 | 배포 실패율 달성 |
- Java/Spring Boot 기반 백엔드 개발, 인증/인가, 배치, 운영 도구, CI/CD 경험
- 장애 원인 분석, 재발 방지, 테스트 도입, 배포 자동화를 통해 운영 리스크 개선
- 문제·원인·해결·성과를 구조화해 공유하고, 재현 가능한 방식으로 시스템 개선

———

# 경력

———

## 엔셀 — DataForge 데이터 처리 플랫폼

📅 2026.01 ~ 현재

Java Spring Boot Redis Elasticsearch JWT OAuth2 MySQL

### 🔴 문제

- 폐쇄망 환경에서 야근 기록을 엑셀로 수기 작성해 상급자가 모바일로 입력·승인하기 어려웠음
- 수동 집계로 인해 관리 부담과 데이터 정합성 문제가 발생
- 피크 타임 Elasticsearch 응답 지연·타임아웃으로 야근 승인/조회 흐름이 중단될 수 있는 상황 발생
- 동시 로그인·토큰 재발급이 빈번한 환경에서 토큰 상태 불일치로 세션 혼선 발생

### 🔵 해결

- ES 장애 감지 조건을 실패율, 응답 시간, 연속 실패 횟수 기준으로 정의하고 DB fallback 자동 전환 구조 구현
- ES는 검색 성능, DB는 정합성 보장 역할로 분리해 장애 상황에서도 핵심 조회 기능이 중단되지 않도록 설계
- ES/DB 조회 결과를 공통 응답 스키마로 통합해 권한 스코프, 정렬, 페이지네이션 정합성 유지
- JWT + Redis 기반 Stateless 인증 구조 설계 — 사용자별 Refresh Token을 Redis에 저장하고 TTL 만료, 재발급 시 기존 토큰 무효화, 로그아웃 시 즉시 폐기 흐름을 구현해 다중 서버 환경에서도 인증 상태를 일관되게 유지

### 🟢 성과

- ES 장애 구간에서도 검색 기능이 완전히 중단되지 않고 DB fallback을 통한 부분 성능 저하 수준으로 완충
- 세션 충돌과 예기치 않은 재로그인 오류를 줄이고, 토큰 발급·폐기 이력을 추적 가능한 구조로 개선
- 수기 엑셀 기반 관리 흐름을 웹 기반 입력·조회·승인 구조로 전환해 운영 편의성 개선

———

## 엔셀 — Prism

📅 2026.02 ~ 현재

Python Postgres Airflow

### 🔴 문제

- 타팀의 Java 기반 FTP 파일 처리 배치를 우리 팀의 Python/Airflow 구조로 마이그레이션해야 했음
- 반복 실행, Airflow 재시도, 수동 재실행 상황에서 중복 처리·누락·재실행 정합성 문제가 발생할 수 있었음
- 자정 경계 시점에 생성되는 파일이 스캔 범위에서 누락될 가능성이 있었음

### 🔵 해결

- Rubi(txt) / Rubp(tif) 도메인으로 처리 흐름을 분리해 파일 유형별 책임과 실패 범위를 명확화
- Airflow DAG 기반 15분 주기 실행 구조로 FTP 수집, 파일 검증, 처리, 이력 저장 흐름 관리
- 파일명 datetime과 file_processing_history 상태값 기반 idempotent 처리 구조 설계 — PROCESSING 선점, DONE 중복 제외, FAIL 재처리 흐름으로 Airflow 재시도·수동 재실행 시에도 중복 적재 방지
- 최근 3일 폴더를 스캔하도록 구성해 자정 경계 및 지연 업로드 파일 누락 방지

### 🟢 성과

- Java 기반 배치를 Python/Airflow 구조로 안정적으로 이관
- 중복·누락 없이 재실행 가능한 파일 처리 구조 구축
- 실패 파일만 선별 재처리할 수 있어 운영 복구 비용 감소

———

## 엔셀 — SMIP 유지보수

📅 2025.01 ~ 2025.12

Java Spring Boot Vue.js JUnit5 Mockito

### 🔴 문제

- API마다 예외 처리 방식이 달라 장애 발생 시 원인 추적 경로가 일관되지 않았음
- 동일 유형 오류가 반복 재발했지만 회귀 테스트가 부족해 재발 방지가 어려웠음
- Vue.js 화면 상태를 각 컴포넌트에서 직접 관리해 화면 복잡도가 증가하고 인수인계가 어려웠음

### 🔵 해결

- 공통 예외 처리 계층과 데이터 검증 정책 설계 — API별로 흩어진 예외 처리를 단일 진입점으로 통합하고, 에러 응답 포맷과 로깅 기준을 표준화해 장애 원인 추적 경로를 단순화
- JUnit5/Mockito 기반 테스트를 도입해 주요 예외 케이스와 회귀 가능성이 높은 로직 검증
- MVVM 패턴 + 공통 스토어 적용 — 화면별 상태를 공통 스토어로 집중시켜 변경 영향 범위를 줄이고, UI 로직과 비즈니스 로직을 분리해 인수인계가 가능한 구조로 정리

### 🟢 성과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 장애 분석 리드타임 | 40분 | 12분 |
| 동일 유형 장애 재발률 | 기준 | 30% 감소 |

> 동일 유형 장애 재발률은 공통 예외 처리 및 테스트 도입 이후 반복 발생한 장애 티켓 기준으로 측정
> 

———

## 헥토 — AI/개발팀

📅 2023.10 ~ 2024.07

Java Spring Boot GitLab CI/CD Docker Nginx OpenAI API LangChain

### 🔴 문제

- 기존 키워드 검색이 문맥을 이해하지 못해 원하는 인터뷰 정보를 찾기 어려웠음
- 수동 배포로 릴리스 리드타임이 길고 휴먼 에러에 지속적으로 노출
- 비정형 데이터 기반 협업으로 API 응답 구조와 검증 방식이 일관되지 않았음

### 🔵 해결

- 스마트큐: LangChain·RAG 기반 사내 문서 벡터 검색 구현 — 키워드 매칭 한계를 보완하기 위해 의미 기반 검색 구조를 도입하고, 기존 인프라 변경을 최소화하는 RAG 구조 선택
- 질의응답 API를 모듈화하고 큐 기반 즉시 응답 구조를 설계해 검색 요청과 응답 처리 흐름 분리
- KMS: GitLab CI/CD + Docker 기반 빌드/테스트/배포 자동화 — Docker로 실행 환경을 고정해 환경 차이로 인한 배포 실패를 줄이고, GitLab CI/CD로 사람 손을 거치지 않는 배포 파이프라인 구성
- Nginx + Staging 환경을 표준화해 배포 전 검증 절차 확립

### 🟢 성과

스마트큐

| 지표 | 전 | 후 |
| --- | --- | --- |
| 질의 응답 정확도 | 60% | 80% |
| 답변 대기 시간 | 5분 | 1분 |
| 상담팀 주당 절감 시간 | — | 12시간 |

> 정확도는 내부 테스트 질의셋 기준 응답 적합성 평가로 측정
> 

KMS

| 지표 | 전 | 후 |
| --- | --- | --- |
| 릴리스 리드타임 | 1시간 | 25분 |
| 배포 실패율 | 5% | 0% |

———

## 헥토 — 세이프캐시 마이데이터개발팀

📅 2022.08 ~ 2023.09

Java Spring Boot Crontab

### 🔴 문제

- 정기 데이터 동기화·배치 작업을 수동으로 수행해 휴먼 에러와 처리 지연이 발생
- 작업 결과 확인 수단이 부족해 이상 발생 시 인지가 늦었음

### 🔵 해결

- Crontab 기반 정기 배치 자동화로 수동 개입 제거
- 작업 실행 이력, 처리 결과, 실패 로그를 남기는 로깅 체계 구축
- Admin UI·API를 고도화해 데이터 조회, 수정, 재처리 흐름을 내부 운영 도구로 통합

### 🟢 성과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 데이터 정합성 이슈 | 월 3건 | 0건 |

———

# 기타 활동

———

## VoiceLink — 실시간 매칭·통화 서비스

📅 2024 ~ 2025 · 개인 사이드 프로젝트

Java Spring Boot Redis LiveKit Docker CI/CD

> 멀티노드 환경의 실시간 매칭 시스템에서 동시성 정합성과 유령 세션 문제를 설계 수준에서 해결한 프로젝트
> 

### 🔵 내용

- 실시간 매칭·통화 기능을 1인 개발로 설계 및 구현
- 취소 직후 stale 매칭 결과가 반복 반환되는 경합 문제 해결
- 세션 객체가 종료 처리되지 않아 재매칭 시 상태 충돌이 발생하는 유령 세션 문제 해결
- 취소 처리 순서를 고정하고 매칭 확정 전 다단계 재확인을 삽입해 상태 전이를 예측 가능하게 구성
- Redis를 멀티노드 간 공유 상태 저장소로 사용하고, TTL 기반 자동 만료와 원자적 연산을 활용
- LiveKit 참여자 수 기반 사전 검사와 stale 기준값 외부 설정을 통해 운영 중 조정 가능한 구조로 개선

———

## Loopers 백엔드 코스

📅 2025.07 ~ 2025.10 · 교육

Java Spring Boot

### 🔵 내용

- Spring Boot 기반 가상 이커머스 플랫폼 설계·구현
- 도메인 모델링, API 설계, 계층 분리, 상황별 기술 선택 학습

———

## 와글와글 소모임

📅 2023.09 ~ 2023.10 · 프로젝트

Java Spring Boot Spring Security OAuth2 JWT

### 🔵 내용

- Spring Security 기반 인증·권한 관리 구현
- 카카오·네이버·구글 OAuth 2.0 소셜 로그인 연동
- JWT 발급, 검증, 세션 관리 흐름 구현

———

GitHub https://github.com/junho0831